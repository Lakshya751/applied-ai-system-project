# assets/

Exported images for the PawPal+ applied AI system.

- `architecture.png` — rendered export of [`../diagrams/architecture.mmd`](../diagrams/architecture.mmd),
  embedded in the project README.

The Mermaid source in `diagrams/` is the source of truth. If the architecture
changes, edit the `.mmd` file first, then re-export the PNG here.
